var simon = require('./../js/game-interface.js').simon;


// function to push user input to userArray

$(function() {
  $('.newGame').click(function() {

    // run compareArrays

      // if gameLoss = false
      
        // run generateColor

        // highlight corresponding div in sequence of simonArray

        // switch turn

        // when user clicks div, put that color into userArray

        // once user div clicks = simonArray.length

    // else show lose message

  });
});
